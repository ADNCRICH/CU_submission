#include <bits/stdc++.h>
using namespace std;
void play(int l, int r, int v) {
}
int main() {
    int n, k;
    cin >> n >> k;
    if (k % 2 == 0 || k >= 1 << n) {
        cout << "-1";
        return 0;
    }
}