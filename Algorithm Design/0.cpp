#include <bits/stdc++.h>
using namespace std;
vector<int> play() {
    return {1, 2, 3};
}
int main() {
    vector<int> a = play();
    for (int i = 0; i < a.size(); i++)
        cout << a[i] << " ";
}
